apiVersion: 1

policies:
  - orgId: 1
    # Must reference an explicitly provisioned contact point when using file provisioning.
    receiver: TelegramBotAlert
    group_by: [grafana_folder, alertname]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 1h
